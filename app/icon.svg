<!-- Placeholder favicon: the brand gradient, so the tab never shows a framework
     default. The supplied logo files (docs/09 item 1) are raster PNGs with no
     square crop, so this stands in until one is cut. -->
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32" width="32" height="32">
  <defs>
    <linearGradient id="g" x1="0" y1="0" x2="1" y2="1">
      <stop offset="0%" stop-color="#F83000"/>
      <stop offset="45%" stop-color="#F85A00"/>
      <stop offset="100%" stop-color="#FFC400"/>
    </linearGradient>
  </defs>
  <rect width="32" height="32" rx="7" fill="#0F0F12"/>
  <rect x="4" y="4" width="24" height="24" rx="5" fill="url(#g)"/>
</svg>
